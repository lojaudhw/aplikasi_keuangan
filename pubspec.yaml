name: aplikasi_keuangan
description: "Aplikasi Pencatatan Keuangan dengan Flutter - Catat Pemasukan dan Pengeluaran"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
